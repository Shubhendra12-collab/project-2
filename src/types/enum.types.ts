export enum Role{
    ADMIN = 'ADMIN',
    USER = 'USER',
    HOST = 'HOST',

}

//PROPERTY PRICE TYPE
export enum PropertyPriceType {
   "PER_HOUR" = 'PER_HOUR',
    "PER_DAY" = 'PER_DAY',
    "PER_WEEK" = 'PER_WEEK',
    "PER_MONTH" = 'PER_MONTH',
    "PER_YEAR" = 'PER_YEAR',
}

//property type
export enum PropertyType {
    "ROOM" = 'ROOM',
    "APARTMENT" = 'APARTMENT',
    "HOUSE" = 'HOUSE',
}